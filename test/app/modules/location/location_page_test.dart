import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:anchor_loans_test/app/modules/location/location_page.dart';

void main() {
  testWidgets('LocationPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(LocationPage(title: 'Location')));
    //  final titleFinder = find.text('Location');
    //  expect(titleFinder, findsOneWidget);
  });
}
